import React from 'react';
const imgUrl = "../assets/bones.png";

const Logo = () => {
    return(
        <div>
            <img src={imgUrl} alt="logo"/>
        </div>
    );
};

export default Logo;
